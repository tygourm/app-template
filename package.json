{
  "name": "app-template",
  "version": "0.0.0",
  "scripts": {
    "build": "mkdocs build -d backend/static/site && cd frontend && pnpm build",
    "lint": "ruff check --no-cache && prettier -c . && cd frontend && pnpm eslint",
    "format": "ruff format --no-cache && prettier -w . && cd frontend && pnpm eslint --fix"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "^3.6.2"
  },
  "packageManager": "pnpm@10.15.0"
}
